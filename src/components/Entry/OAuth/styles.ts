import { createUseStyles } from 'react-jss';

export default createUseStyles({
    'oauth-container': {
        textAlign: 'center',
        margin: '30px 0 5px',
        '& svg': {
            margin: '0 5px',
            cursor: 'pointer',
        }
    }
});
